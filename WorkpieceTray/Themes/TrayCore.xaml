<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:WorkpieceTray">
    <Style TargetType="{x:Type local:TrayCore}">
        <!--<Setter Property="GradientShow" Value="True" />
    <Setter Property="RealGradientShow" Value="True" />
    <Setter Property="SpeedShow" Value="True" />
    <Setter Property="PressureShow" Value="True" />
    <Setter Property="GradientShow" Value="True" />
    <Setter Property="RealSpeedShow" Value="True" />-->
        <Setter Property="Margin" Value="0,5,0,0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:TrayCore}">
                    <Grid Name="Part_MarinG">
                        <Image x:Name="Part_PlotImage" ContextMenu="{TemplateBinding Menus}" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>